'use client';

// Scripts are loaded sequentially via the inline chain in layout.tsx.
// main.js and countdown.js load AFTER all vendor libs in the correct order.
export default function ScriptsLoader() {
  return null;
}
